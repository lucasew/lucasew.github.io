---
import UtilsHomeLayout from '../../../../layouts/UtilsHomeLayout.astro'
import { getEntry, asStringArray } from '../../../../lib/content'
import { getStaticPathsForLangs, type Lang } from '../../../../lib/i18n'

export function getStaticPaths() {
  return getStaticPathsForLangs()
}

const lang = Astro.params.lang as Lang
const entry = getEntry(lang, ['utils', 'home'])
if (!entry) {
  return Astro.redirect(`/${lang}/`)
}
---

<UtilsHomeLayout
  lang={entry.lang}
  title={entry.title}
  quotes={asStringArray(entry.frontmatter.quotes)}
/>
