[env]
_.path = ["./node_modules/.bin"]

[tools]
"github:lucasew/workspaced" = "0.4.2"
node = "25.4.0"

[tasks.lint]
run = "workspaced codebase lint"

[tasks.fmt]
run = "workspaced codebase format"

[tasks.test]
depends = ["test:*"]

[tasks."test:npm"]
run = "npm run check"

[tasks.install]
depends = ["install:*"]

[tasks."install:npm"]
run = "npm install"

[tasks."codegen:data"]
description = "Run update.sh"
run = "./update.sh"

[tasks.codegen]
description = "Generate all code"
depends = ["codegen:*"]

[tasks.ci]
depends = ["lint", "test"]
